name: notebook
channels:
  - eoas_ubc
  - conda-forge
  - defaults
dependencies:
  # NOTE: pip required for running tests in this repo
  - pip=20
  - fortran-compiler
  - cmake
  - make
  - libgcc-devel_linux-64=9.3.0
  - python=3.8
# conda-lock -f environment.yml -p linux-64
